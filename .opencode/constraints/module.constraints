# 模块专项约束

[metric_field]
- 正定性由Cholesky分解保证: g = L*L^T + eps*I
- 不允许使用任何其他参数化方式（如特征值分解）
- 输入坐标归一化到[0,1]
- 每个像素存储3个参数: l11, l21, l22
- 必须实现双线性插值采样

[atoms]
- 截断函数必须使用smoothstep, δ=0.2, C2连续
- 原子形状完全由局部度量决定, 禁止独立协方差矩阵
- 截断范围外严格为零
- 半径参数使用对数参数化: r = exp(log_r)
- 存在概率使用logit参数化: eps = sigmoid(logit_eps)

[renderer]
- 初期纯PyTorch实现, 不使用CUDA kernel
- 体积积分使用标准alpha合成公式
- 必须支持梯度反向传播到原子参数

[losses]
- 凝聚损失必须包含抑制特征坍缩的机制（如惩罚项或熵奖励）
- 度量平滑损失使用有限差分计算梯度
- 不使用任何外部语义标注或预训练模型

[data]
- 使用OpenCV绘制基础形状
- 生成多视图通过随机仿射变换模拟相机运动
- 输出归一化到[0,1]的RGB图像
