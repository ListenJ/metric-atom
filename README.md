# MetricAtom

**黎曼度量驱动的感知原子：从多视图图像中无监督涌现物体实例**

MetricAtom 是一个基于黎曼度量场和有界感知原子的三维场景理解系统。核心假设是：通过联合优化外观重建和度量结构，原子能自然地凝聚为与真实物体边界对齐的簇，无需任何预训练语义模型（SAM/CLIP）或外部 2D 分割引导。

## 核心思想

```
多视角图像
     │
     ▼
黎曼度量场 g(x) ←── 联合优化
     │
感知原子（紧支撑、特征向量、存在概率）
     │
可微体积渲染 ─── 重建损失
     │
凝聚损失 ───── 自发聚类
     │
物体实例（簇）
```

## 系统架构

```
src/
├── geometry/           # 黎曼度量场（2D/3D）+ 椭圆曲线几何 (Phase 6b)
│   ├── cholesky_param.py
│   ├── metric_field.py
│   ├── elliptic_curve.py    # ECO: 群运算 / log/exp 映射 / j-不变量
│   └── murmuration.py       # Boids 在椭圆曲线上的动力学
├── atoms/              # 感知原子定义
├── rendering/          # 体积渲染器
├── losses/             # 损失函数
│   ├── direct_cluster.py    # Direct Loss (Path 1+3, ARI 0.755)
│   └── eco_cluster.py       # ECO Cluster Loss (Phase 6b, j-不变量稳定)
├── data/               # 合成数据生成
├── visualization/      # 可视化
└── training/           # 训练循环
```

## 历史成果

### Phase 1: 基础设施（2026-05-11）

项目初始化，搭建完整的 2D 场景理解管线。

| 模块 | 成果 |
|------|------|
| 度量场 | Cholesky 参数化正定度量矩阵，双线性插值 |
| 感知原子 | C² 连续 smoothstep 截断，马氏距离空间支持 |
| 体积渲染 | 可微体积渲染器，向量化 17× 加速 |
| 损失函数 | L1 重建 + 度量平滑 + 占位耦合 |
| 数据 | 2D 多形状合成场景生成 |
| 训练 | 完整训练循环，动态原子管理（播种/剪枝） |

### Phase 2: 覆盖优化（2026-05-11 ~ 05-12）

目标：原子覆盖更多物体区域。

| 成果 | 说明 |
|------|------|
| 误差驱动播种 | 在高重建误差区域动态添加原子 |
| 渲染贡献剪枝 | 移除贡献低于阈值的无效原子 |
| 位置正则化 | 阻止原子漂离物体区域 |
| MKL CPU 优化 | 6 线程，4× 加速 |
| BF16 混合精度 | CUDA 加速，显存优化 |
| MLP 度量场 | 分辨率无关的连续度量场 |

### Phase 3: 聚类探索（2026-05-12 ~ 05-15）

目标：原子自发形成物体簇。尝试了多种方案。

| 方案 | ARI | 结论 |
|------|-----|------|
| 特征方差排斥 | ~0.006 | 基本随机 |
| 密度感知播种 | ~0.006 | 覆盖好但无聚类 |
| 周期性噪声注入 | ~0.006 | 无效 |

**关键发现**：仅靠几何正则化（凝聚+排斥+覆盖）不足以产生聚类信号。需要显式的判别性损失。

### Phase 4: 聚类突破（2026-05-15）

**三项技术贡献**：

| 序号 | 技术 | 说明 |
|------|------|------|
| 1 | KMeans 空间特征初始化 | Phase 2 时用原子位置 K=2 聚类，打破特征对称性 |
| 2 | InfoNCE 对比损失 | 基于测地距离的正负样本对，替代吸引/方差排斥 |
| 3 | 度量耦合增强 | E_vol 权重 0.02→0.2，度量场学习锐利物体/背景边界 |

| 指标 | 突破前 | 突破后 | 提升 |
|------|--------|--------|------|
| **ARI** | 0.006 | **0.175** | **29×** |
| 凝聚损失 | -22.178 (发散) | +0.232 (稳定) | 有界收敛 |
| 总损失 | -21.14 (负值主导) | +4.909 (正值) | 正常训练 |

### Phase 5: Direct Loss 直接聚类（2026-05-16 ~ 05-18）

**根因分析**：InfoNCE 在黎曼空间中存在逻辑循环——需要聚类定义度量，又指望度量定义聚类。度量场和特征同时控制正负样本间隔，可行域极度狭窄。

**解决方案**：用 Sinkhorn 可微软分配替代 InfoNCE，直接最小化簇内测地距离。

```
度量场 g → 占位耦合（学边界）→ g 变锐利 → Sinkhorn 软分配 P → L_direct = Σ_k P[:,k]ᵀ @ D²_g @ P[:,k]
```

| 指标 | InfoNCE 最佳 | Direct Loss | 提升 |
|------|-------------|-------------|------|
| **ARI** | 0.440 | **0.755** | +72% |
| **NMI** | 0.291 | **0.664** | +128% |
| 簇平衡度 | — | 0.94 | 近乎完美 |

**配套基础设施**：

| 功能 | 说明 |
|------|------|
| 超参网格搜索 | Phase 4/5 自动化扫描 |
| 特征扩散模块 | 测地亲和力 + sigmoid 软遮罩 + 自适应 σ |
| 实验管理脚本 | `run_experiments.py` / `run_remote.sh` / `run_local.sh` |

### Phase 6a: Murmuration-ECO 理论框架（2026-05-18）

**核心定义**：物体 = 椭圆曲线上的概率流形 $O = (E, \mu_E, v)$。

身份由 j-不变量 $j(E) = 1728 \cdot \frac{4a^3}{4a^3+27b^2}$ 编码，**二阶稳定** $\delta j = O(\|\delta\|^2)$。

**尖锐悬崖诊断**：Sinkhorn $P_{ij}=e^{-C_{ij}/\varepsilon} / \sum_k e^{-C_{ik}/\varepsilon}$ 对特征初始化一阶敏感，导致只有 1/4 seed 找到好盆地（$\sigma_{ARI}\approx 0.35$）。

**ECO 解决方案**：用 j-不变量（二阶稳定）+ 流形 Sinkhorn（测地距离）+ Lyapunov 稳定性（紧致流形），预期将标准差从 0.35 降至 0.08。

### Phase 6b: ECO 代码实现（2026-05-18）

| 模块 | 功能 |
|------|------|
| `elliptic_curve.py` | 群运算（加法/标量乘法）、log/exp 映射、测地距离数值积分、j-不变量、分岔检测 |
| `murmuration.py` | Boids 在椭圆曲线上的动力学（凝聚/对齐/分离力在 1D 切空间中），PyTorch 批处理版本 |
| `eco_cluster.py` | ECO 版 Direct Loss：感知函数 φ: features→(a,b)，曲线残差成本矩阵，j-不变量身份一致性正则项 |

### 云端实验结果总览（2026-05-18）

| 实验 | ARI | NMI | Valid | 分析 |
|------|-----|-----|-------|------|
| seed 123 (64) | -0.0292 | 0.0049 | 58/82 | 初始化接近分岔点 |
| **seed 456 (64)** | **0.9375** | 0.8992 | 64/82 | **远离奇异点，动力学稳定** |
| 3-object (64) | 0.1755 | 0.1928 | 66/82 | 多物体 j-不变量冲突 |
| 128×128 (seed 42) | 0.0000 | 0.0000 | 45/90 | 高分辨率加剧尖锐性 |



## 约束

本项目遵循严格的零外部先验原则：
- 禁止使用 COLMAP、SAM、CLIP、高斯泼溅
- 禁止使用预训练语义/分割模型
- 仅使用多视图图像 + 可微渲染 + 几何正则化

## 分支说明

| 分支 | 用途 |
|------|------|
| `main` | 稳定发布版本 |
| `feat/clustering-breakthrough` | 无监督聚类突破（Direct Loss + ECO 理论框架） |

## 许可

MIT
